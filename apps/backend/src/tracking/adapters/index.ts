/**
 * Provider adapter assembly point. Exposes the adapter contract + versioned
 * registry; concrete provider adapters (Tasks 3-5) are registered here.
 */
export * from './tracking-provider.adapter';
